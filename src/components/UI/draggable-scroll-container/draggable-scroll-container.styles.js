import styled from "styled-components";

export const ScrollContainer = styled.div`
  position: relative;
  display: flex;
  gap: 10px;
  flex-wrap: no-wrap;
  overflow: hidden;
  white-space: nowrap;
  cursor: grab;
`;
